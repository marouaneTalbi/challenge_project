{% extends 'base_front.html.twig' %}
{% block title %}Password Reset Email Sent{% endblock %}
{% block body %}
    <div class="bigContainer">
        <div class="containerReset">
            <h1 class="mainTitle">Password Reset Email Sent</h1>
            <p class="reset_email_message">
                If an account matching your email exists, then an email was just sent that contains a link that you can use to reset your password.
                This link will expire in {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.
            </p>
            <p class="reset_email_message">If you don't receive an email please check your spam folder or <a href="{{ path('front_forgot_password_request') }}">try again</a>.</p>
        </div>
    </div>
{% endblock %}
